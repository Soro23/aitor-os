import { PlaceholderSection } from "../_components/PlaceholderSection";

export default function NowPage() {
  return (
    <PlaceholderSection
      eyebrow="Now"
      title="Now"
      description="Trabajando en / Aprendiendo / Explorando ahora mismo. Se conecta a la tabla now_items en la Fase 8."
      accent="green"
    />
  );
}
