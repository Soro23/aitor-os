import { PlaceholderSection } from "../_components/PlaceholderSection";

export default function RecursosPage() {
  return (
    <PlaceholderSection
      eyebrow="Recursos"
      title="Recursos"
      description="Herramientas, librerías, cursos, libros y referencias técnicas. Se conecta a la tabla resources en la Fase 7."
      accent="violet"
    />
  );
}
