import { PlaceholderSection } from "../_components/PlaceholderSection";

export default function ProyectosPage() {
  return (
    <PlaceholderSection
      eyebrow="Proyectos"
      title="Proyectos"
      description="Listado de proyectos publicados, con ficha individual por proyecto (problema, solución, tecnologías, estado, aprendizajes). Se conecta a la tabla projects en la Fase 5."
      accent="cyan"
    />
  );
}
