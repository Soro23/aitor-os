# Variables para supabase/docker-compose.yml (stack de PRODUCCION en
# Coolify) — no confundir con el .env.example de la raiz, que es para la
# app Next.js. Generar valores propios, nunca reutilizar estos ejemplos.
POSTGRES_PASSWORD=
JWT_SECRET=
ANON_KEY=
SERVICE_ROLE_KEY=
SITE_URL=https://aitor-os.example.com
